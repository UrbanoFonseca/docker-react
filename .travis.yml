sudo: required
services:
    - docker

before_install:
    - docker build -t urbanofonseca/docker-react -f Dockerfile.dev .

script:
    - docker run -e CI=true urbanofonseca/docker-react npm run test -- --coverage